import { RegisterStepTwo } from "../../../modules/auth/ui";
import { Text, TouchableOpacity, View } from "react-native";
import { Link } from "expo-router";

export default function StepTwo() {
	return (
		<View style={{flex: 1}}>
			<RegisterStepTwo />			
		</View>
	);
}